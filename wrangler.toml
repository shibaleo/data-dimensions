name = "data-dimensions"
main = "dist/_worker.js"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat_v2"]

# TODO: deploy 時に新しい Hyperdrive インスタンスを作成して id を差し替える
# (今は data-drills-cf のものをそのまま流用 — local dev では使われない)
[[hyperdrive]]
binding = "HYPERDRIVE"
id = "ec8131aac5e64775864336ec25f1267b"

[assets]
directory = "dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
